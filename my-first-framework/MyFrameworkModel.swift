import SwiftUI
import Combine

public struct MyFrameworkModel {
    public var slug: String
    public var channelId: String
    public var message: String = "Hello from the SDK"
    public var text: String = "We are in the SDK now"
}
